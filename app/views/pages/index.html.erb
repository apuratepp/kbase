<%= render 'nav' %>

<table class="table table-striped table-condensed table-bordered">
<% @pages.each do |page| %>
  <tr>
  	<td class="icon"><%= icon('icon-file').html_safe %></td>
    <td><%= link_to page.title, page %></td>
    <td><%= distance_of_time_in_words_to_now page.updated_at %> ago</td>
</td>
  </tr>
<% end %>
</table>

<br />

